/**
 * Tipos generados desde PostgreSQL — MARCADOR DE POSICIÓN.
 *
 * Este archivo se sobreescribe con `pnpm db:types` (supabase gen types) cuando exista
 * el esquema de la Fase 2. No editar a mano: el generador lo reemplaza por completo.
 */
export interface Database {
  public: {
    Tables: Record<string, never>;
    Views: Record<string, never>;
    Functions: Record<string, never>;
    Enums: Record<string, never>;
  };
}
